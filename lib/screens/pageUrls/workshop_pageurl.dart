import 'package:flutter/material.dart';

class WorkshopPageurl extends StatelessWidget {
  const WorkshopPageurl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "WorkshopPageurl",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
