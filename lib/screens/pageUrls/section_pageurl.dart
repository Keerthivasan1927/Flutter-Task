import 'package:flutter/material.dart';

class SectionPageurl extends StatelessWidget {
  const SectionPageurl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "SectionPageurl",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
