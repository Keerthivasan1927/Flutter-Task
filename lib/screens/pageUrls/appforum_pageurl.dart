import 'package:flutter/material.dart';

class AppforumPageurl extends StatelessWidget {
  const AppforumPageurl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "AppforumPageurl",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
