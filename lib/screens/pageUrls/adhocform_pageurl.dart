import 'package:flutter/material.dart';

class AdhocFormScreen extends StatelessWidget {
  const AdhocFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "AdhocForm",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
